name: QTS A4 Obsolete Hotfix Archive

on:
  workflow_dispatch:

jobs:
  archived:
    runs-on: ubuntu-latest
    steps:
      - run: echo "A4 V2.5 hotfix workflow retired. Formal production is scraper.yml V3."
